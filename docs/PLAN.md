# ButFun — 現在主攻(Planner 導航)

> 四條工程線**開工前先讀 30 秒**的當前優先序。
> **總監 2026-06-05 定調:界線往「可玩」壓——衝可玩、一直做下去。** Phase 1 玩法接線
> 【是主攻,不算跳級】;地基/持久化【並行、別擋路】。唯一守的紀律是「薄切片」。
> 詳細清單見 `docs/BACKLOG.md`。

## ⚠️ 本輪校正:診斷對了卻【又一輪 0 落地】——問題不在「該做什麼」,在「怎麼真的 merge」

上輪已正確指出:`gather.rs`/`inventory.rs` 早在 main 當死碼、繞開 #17 直接接 `ws.rs` 即可。
**結果:又過了一輪,`main.rs` 仍只有 `mod gather/inventory/gather_field;`、`ws.rs`/`game.rs` 一行都沒呼叫。**
PR #13/#15/#17 全部自昨天起無動靜、零 merge、#17 還漲到 15 commits。**指示沒錯,是執行卡死。**

**鐵腕換失敗模式:別再 rebase / 救那三個卡衝突的舊 PR。** 從 **最新 main 開一條乾淨新枝**,
寫**最小到不可能衝突**的接線(~40 行)、單一 PR、立刻 merge。介面都現成(見下),這是抄寫不是設計。

## 🎯 現在主攻:把「採集 → 背包」接進 `ws.rs`,**砍到骨、純記憶體、本輪 merge**

最小迴圈只要做到:**走近固定節點 → 按鍵 → 數字進背包 → 快照看得見**。
**重生、持久化、散佈、UI 美化全部不在這條**——它們是切片 2,別讓任何一個擋住第一次落地。

介面已就緒(直接呼叫即可,不必改它們):
- `gather.rs`:`ResourceNode::new(kind)`、`gather() -> Option<u32>`(回傳採到的量)、`is_harvestable()`。
- `inventory.rs`:`Inventory::new()`、`add(item, qty)`、`entries()`(給快照序列化)。

## 接下來的切片(薄切片快速連發,由上往下)

1. **薄採集→背包【純記憶體】**(主攻,本輪必 merge):`Player` 加 `Inventory`;`game.rs` 世界塞
   **3~5 個固定 `ResourceNode`**;`ws.rs` 加一個 `Gather` 動作(找最近可採節點→`gather()`→`add()`);
   `Snapshot` 加 `inventory`(`entries()`)+`nodes` 兩欄。**不做重生、不碰 DB、不改前端美術。**
   驗收 = 玩家走到節點旁按鍵 → 看到背包數字 +1。能 merge 比好看重要一百倍。
2. **節點重生 + 散佈**:每 tick 呼 `node.tick(dt)` 讓採完的節點按 `respawn_secs` 回來;接 #13 的
   `node_scatter` 讓節點滿地長。驗收 = 採完會空、過一陣子又能採。
3. **背包持久化**:`Inventory`/節點存 Postgres(用現成 `from_raw`/`is_loadable`)。前端新欄位 `|| []` 防呆。
   驗收 = 重連背包還在。
4. **改名薄切片**(並行、高優先):設定面板改名 → `PATCH /api/profile`(過 `sanitize_name`)→ 即時反映
   HUD/聊天。後端 `UserStore::rename` 已上線,缺 live 廣播 + 前端面板。驗收 = 改名後自他即時看到、重連仍新名。
5. **物品有用途 / 戰鬥 MVP**:採到的能賣乙太/餵作物/當材料;之後自動打怪掉落進背包。**等 1 落地再開。**

## 給各線的具體指示(分工清楚 = 少撞檔)

- **backend(主力,只做切片 1)**:**從最新 main 開乾淨新枝**,別碰 #13/#15/#17。`Player` 加 `Inventory`、
  `game.rs` 擺幾個固定節點、`ws.rs` 接一個 `Gather` 動作、`Snapshot` 加兩欄。**純記憶體、不做重生/持久化**。
  目標是**今天就 merge 一條最小可玩迴圈**。動 live 廣播 shape 走 PR、前後端同版。
- **feature**:**停手做新系統,先把 #17 關掉/砍乾淨**(gather/inventory 與 main 重複、其餘是跳級垃圾)。
  想出力就寫「內容資料」(節點擺哪、`NodeKind` 掉什麼、機率),**別碰 `ws.rs` 接線**(那是 backend 的)。
- **frontend**:**別再 rebase #15**——若衝突難解就**從 main 重開乾淨枝**做兩件小事:背包面板(按 I/點圖示開關、
  讀 `snapshot.inventory || []`)+ 採集飄字;以及**改名設定面板**(高優先)。**農地打磨到此為止。**
- **feedback**:採集→背包未 merge 前**寧可早退**,別回頭磨田。落地後再挑「一眼看懂新玩法」的小提示
  (節點可採提示、背包怎麼開)。

## 暫緩(現在先別做)

- **救 #13/#15/#17 的舊衝突**——別再投時間 rebase,從 main 重開乾淨小枝更快。**#17 直接關掉/砍到不留死碼。**
- **節點重生 / 持久化 / node_scatter**——是切片 2,別塞進第一次落地的 PR。
- **載具 / 飛船 / 多星球 / 競速**(`vehicle.rs`/`ride_registry.rs`)——真正遠期,不碰。
- **戰鬥 / 合成 / 工具 / vitals 系統**——等採集→背包真正 merge 後再分薄切片排。
- **農地任何進一步微打磨**——已足夠,再磨就是範圍蔓延。
- 一次把多個系統塞進同一個 PR——薄切片一條條來、各自能 merge。範圍蔓延+整合衝突仍是頭號殺手。
