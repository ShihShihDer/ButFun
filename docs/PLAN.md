# ButFun — 現在主攻(Planner 導航)

> 四條工程線**開工前先讀 30 秒**的當前優先序。
> **總監 2026-06-05 定調:界線往「可玩」壓——衝可玩、一直做下去。** Phase 1 玩法接線
> 【是主攻,不算跳級】;地基/持久化【並行、別擋路】。唯一守的紀律是「薄切片」。
> 詳細清單見 `docs/BACKLOG.md`。

## ⚠️ 本輪校正:整整一輪過去,玩法鏈仍 0 接線——而且阻塞點被誤判了

上輪叫 backend「從 #17 抽出 `gather.rs`+`inventory.rs` 接進 `ws.rs`」。**這前提是錯的**:
- **`gather.rs`、`inventory.rs`(連 `gather_field.rs`、`crops.rs`)早就在 main 上**躺著當死碼,
  根本不必等 #17。`gather.rs` 自帶 node 邏輯、**不依賴 #17 任何檔**。
- 結果:三個 PR(#13/#15/#17)整整一天**全 OPEN、全與 main 衝突、零 merge**;main 只多了
  #16(ops)+ 一行 feedback HUD。**玩法鏈一行都沒接進 `ws.rs`。**
- **#17 仍是 14 檔大坑**(combat/crafting/vehicle/ride/tools/vitals…),其唯一有用的 gather/inventory
  **與 main 重複**。它已無接線價值、只剩跳級垃圾與衝突風險。

**鐵腕收斂:backend 直接拿 main 上的 `gather.rs`/`inventory.rs` 接 `ws.rs`,完全繞開 #17。
建議直接關掉 #17**(它沒有 main 沒有的東西,只夾帶遠期跳級)。別再把「PR 開著」當成「有進度」。

## 🎯 現在主攻:把「採集 → 背包」接進 `ws.rs` 並真正 merge 進 main

最小迴圈:走近節點 → 按鍵採到 → 進背包 → 看得見 → 跨重連還在。
**地基已在 main**(`gather.rs`/`inventory.rs`),缺的純粹是 `ws.rs` 接線 + 落地。沒接 = 沒進度。

## 接下來的切片(薄切片快速連發,由上往下)

1. **薄採集→背包落地**(主攻):`Player` 帶背包欄位,`ws.rs` 加「採集」動作(走近最近節點觸發
   `gather.rs`)、快照帶背包+節點、每 tick 推進節點重生。世界先擺幾個固定節點即可上線;
   #13 的 `node_scatter`(滿地散佈)可隨後並行補,**別讓它擋住第一條落地**。
   驗收 = 玩家走到節點旁按鍵 → 木/石/乙太進可開關背包 → 重連還在。**戰鬥/合成/載具一律不在這條。**
2. **改名薄切片**(高優先、並行):設定面板改名輸入 → `PATCH /api/profile`(過 `sanitize_name`)
   → 即時反映 HUD/聊天。後端 `UserStore::rename` 已上線,缺 live 廣播 + 前端面板。
   驗收 = 改名後自己與他人即時看到、重連仍是新名。
3. **物品有用途**:採到的東西能賣乙太 / 餵作物 / 當材料,讓背包不只是清單(接上 `economy.rs` 的 sink)。
4. **戰鬥 MVP(自動打怪)**:世界有可打目標、自動攻擊、掉落進背包。**等 1 落地後再開**,先最薄一條。
5. **(並行)持久化**:背包/位置/農地存 Postgres——能先上就先上、隨後補(快照新欄位前端 `|| []` 防呆)。

## 給各線的具體指示(分工清楚 = 少撞檔)

- **backend(主力)**:這輪只做一件事——**把 main 上現成的 `gather.rs`/`inventory.rs` 接進 `ws.rs` 並讓它真的 merge**。
  `Player` 加背包、`ws.rs` 加「採近處節點」動作、快照帶背包+節點、迴圈推進重生。世界先擺幾個
  固定節點就能上線。**繞開 #17**;`node_scatter`(#13)當第二步並行。動 live 廣播 shape 走 PR、前後端同版。
- **feature**:**停手做新系統**。**關掉 / 大砍 #17**——它的 gather/inventory 與 main 重複、其餘是跳級垃圾。
  想出力就寫「內容」(節點怎麼擺、掉什麼、機率),**別碰 `ws.rs` 接線**(那是 backend 的,避免撞 `main.rs`)。
- **frontend**:rebase #15 解衝突落地。`web/game.js` 做**背包面板**(按 I/點圖示開關)+採集飄字,
  以及**改名設定面板**(高優先)。新快照欄位一律 `|| []` 防呆。**農地打磨到此為止。**
- **feedback**:採集/背包未落地前**寧可早退**,別回頭磨田。落地後再挑「讓新玩法一眼看懂」的小提示
  (節點可採提示、背包怎麼開)。

## 暫緩(現在先別做)

- **#17 整包**——已無接線價值(gather/inventory 與 main 重複),其餘是跳級垃圾,**本輪請關掉或砍到不留死碼**。
- **載具 / 飛船 / 多星球 / 競速**(`vehicle.rs`/`ride_registry.rs`)——真正遠期,不碰。
- **戰鬥 / 合成 / 工具 / vitals 系統**——等採集→背包真正 merge 後再分薄切片排。
- **農地任何進一步微打磨**——已足夠,再磨就是範圍蔓延。
- 一次把多個系統塞進同一個 PR——薄切片一條條來、各自能 merge。範圍蔓延+整合衝突仍是頭號殺手。
- 捏角色全套、行銷——遠期。
