# ButFun — 現在主攻(Planner 導航)

> 四條工程線**開工前先讀 30 秒**的當前優先序。
> **總監 2026-06-05 定調:界線往「可玩」壓——衝可玩、一直做下去。** Phase 1 玩法接線
> 【是主攻,不算跳級】;地基/持久化【並行、別擋路】。唯一守的紀律是「薄切片」。
> 詳細清單見 `docs/BACKLOG.md`。

## ⚠️ 本輪校正:三條線都動了,但卡在「大坑 + 衝突」、玩法鏈仍 0 接線

好消息:各線終於開始寫玩法,不再磨田。壞消息:**沒有一條落地**——
- main 自上輪只多了 PR #16(AI 註冊端點,屬 ops),**採集→背包在 `ws.rs`/`game.rs` 仍 0 行接線**。
- #13 / #15 / #17 三個 PR **全 OPEN、全與 main 衝突待整合**。
- **#17(feature)是範圍蔓延大坑**:一口氣動 combat/crafting/economy/enemy/gather/inventory/
  **vehicle/ride**/tools/vitals 共 14 檔 —— 正是規則明令禁的「一次做完整系統」,還夾帶**載具
  (遠期跳級)**,而且**沒碰 `ws.rs`**,等於又疊一堆沒接線的死碼。

**鐵腕收斂:#17 拆小、砍掉載具/戰鬥/合成,只留最薄一條採集→背包先 rebase 落地。**
別讓「寫得多」假裝成「做得完」——沒接進 `ws.rs` 權威迴圈、沒上 main = 沒進度。

## 🎯 現在主攻:把「採集 → 背包」**這一條**接進 `ws.rs` 並真正 merge 進 main

只要這條最小迴圈:走近節點 → 採到 → 進背包 → 看得見 → 跨重連還在。
地基(`gather.rs`/`inventory.rs`/node_scatter)早已寫好,缺的只有**接線 + 整合落地**。

## 接下來的切片(薄切片快速連發,由上往下)

1. **薄採集→背包落地**(主攻):從 #17 抽出 `gather.rs`+`inventory.rs`,接進 `ws.rs`(加「採集」
   動作、快照帶背包+節點),合上 #13 的 `node_scatter`,rebase 解衝突後 merge。
   驗收 = 玩家走到節點旁按鍵 → 木/石/乙太進可開關背包 → 重連還在。**戰鬥/合成/載具一律不在這條。**
2. **改名薄切片**(高優先、並行):設定面板改名輸入 → `PATCH /api/profile`(過 `sanitize_name`)
   → 持久化 → 即時反映 HUD/聊天。後端據記憶已寫好(`UserStore::rename`),缺 live 廣播+前端面板。
   驗收 = 改名後自己與他人即時看到、重連仍是新名。
3. **物品有用途**:採到的東西能賣乙太 / 餵作物 / 當材料,讓背包不只是清單。
4. **戰鬥 MVP(自動打怪)**:世界有可打目標、自動攻擊、掉落進背包。**等 1 落地後再開**,先最薄一條。
5. **(並行)持久化**:背包/位置/農地存 Postgres——能先上就先上、隨後補。

## 給各線的具體指示(分工清楚 = 少撞檔)

- **backend(主力)**:這輪只做一件事——**把採集→背包接進 `ws.rs` 並讓它真的 merge**。
  先 rebase #13(node_scatter)解掉與 main 的衝突;`Player` 帶背包、`ws.rs` 加走近最近節點的
  「採集」動作、快照帶背包+節點、迴圈每 tick 推進重生。**動 live 廣播 shape 走 PR、前後端同
  版、別在共用工作樹留半成品。**
- **feature**:**停手做新系統**。把 #17 砍到只剩採集/背包需要的 `gather.rs`/`inventory.rs`,
  **撤掉 vehicle/ride/combat/crafting/tools/vitals**(那些是跳級+大坑,留待 3/4 切片再分批)。
  寫「內容」(節點怎麼擺/掉什麼)可以,但**別碰 `ws.rs` 那段接線**(那是 backend 的,避免再撞 main.rs)。
- **frontend**:rebase #15 解衝突落地。`web/game.js` 做**背包面板**(按 I/點圖示開關)+採集飄字,
  以及**改名設定面板**(高優先)。新快照欄位一律 `|| []` 防呆。**農地打磨到此為止。**
- **feedback**:採集/背包未落地前**寧可早退**,別回頭磨田。落地後再挑「讓新玩法一眼看懂」的小
  提示(節點可採提示、背包怎麼開)。

## 暫緩(現在先別做)

- **戰鬥 / 合成 / 工具 / vitals 系統**——從 #17 撤出,等採集→背包這條真正 merge 後再分薄切片排。
- **載具 / 飛船 / 多星球 / 競速**(`vehicle.rs`/`ride_registry.rs`)——**真正遠期,本輪請從 PR 移除。**
- **農地任何進一步微打磨**——已足夠,再磨就是範圍蔓延。
- 一次把多個系統塞進同一個 PR——**薄切片一條條來、各自能 merge**,範圍蔓延+整合衝突是頭號殺手。
- 捏角色全套、行銷——遠期(改名是 1.5-A 最小先行步)。
