# ButFun — 現在主攻(Planner 導航)

> 四條工程線**開工前先讀 30 秒**的當前優先序。
> **總監定調:衝可玩、一直做下去。** 唯一守「薄切片」。

## 🎯 現在主攻:武器接線 — 零件已全備,只差 game.rs 一行

玩法鏈是 **採集→背包→裝備/物品用途→戰鬥**。戰鬥已全上線(自動鎖定/反擊/扣血/打趴/
掉落進背包),且 PR #27「活起來的世界」已 land——敵人會巡邏、主動追擊你。但中間「**武器**」
這環沒接:`game.rs:174` 仍把傷害寫死成 `PLAYER_ATTACK_POWER`(徒手值 2),**玩家對戰鬥
零選擇**、被追上來只能徒手挨打。補上武器=閉合「採集→合成→變強打怪」正回饋圈。

> **本輪重大更正**:上份 PLAN 說「武器件 feature 沒做、維護者連寫帶接整套」——**過時了**。
> backend 線已把武器整套純邏輯 land 進 main(以「內部清理/對齊」名義):`inventory.rs`
> 有 `ItemKind::Weapon`、`combat.rs` 有 `weapon_power(&inv)` 查表+`UNARMED/WEAPON_ATTACK_POWER`
> 常數+測試、`crafting.rs` 有武器配方(`id:"weapon"` → `ItemKind::Weapon`)。**零件 100% 齊。**
> 維護者**不要再寫任何武器邏輯**,只剩**一行接線**。

### 給維護者的接線優先序(一次一條,別並行撞 main)
1. **武器接線 — game.rs 一行(本輪第一優先,極小)**:`game.rs:158` 收集 `positions` 時
   (已持 `players` 讀鎖)順手帶上每位玩家的 `combat::weapon_power(&p.<背包>)`,把 `:174`
   的 `attack_nearest(*px,*py, PLAYER_ATTACK_POWER)` 換成用該值。背包已 per-player 持久化+
   廣播,inv 拿得到。**additive、無新 protocol、不碰廣播 shape、騎現有 craft+背包面板** →
   合成出武器就立刻打怪更痛。最低風險、最快見效。
2. **擴地(0-G-O2)**:`protocol` 加 `ClientMsg::BuyExpansion`、`ws.rs` 收下、`game.rs` 查
   `economy::expansion_cost` 扣乙太、給該玩家多一塊相鄰地塊。純邏輯+前端面板已備。複用既有
   per-player `fields` Vec 廣播(新地塊=多一個 Field)最低風險;真要動 shape 才走 PR。**架構級,排第二。**
3. **載具(1-E)上下車**:純邏輯已在 `auto/feature`(騎乘登記/上車偵測/step_ridden),維護者
   **先摘回 main** 再接上下車意圖。給世界移動爽感+探索。

## 接下來 1-3 個切片(每個附「驗收=玩家能做到什麼」)

1. **武器接線(一行,先 land)**:合成出的武器讓打怪明顯更痛(5 vs 徒手 2)。
   **驗收 = 身上有武器時自動戰鬥傷害變高、打怪更快**,戰鬥第一次有玩家選擇。
2. **擴地 MVP(0-G-O2)— 乙太的去處**:固定下一格、逐格漲價、無市場。
   **驗收 = 玩家花乙太多拿一塊相鄰地塊**,收成/打怪的乙太終於有去處(GDD「產出要有去處」閉合)。
3. **載具 MVP(1-E)— 移動爽感**:走近按鍵上車、3 倍速+輕微甩尾、下車回走路。先一台車一人騎。
   **驗收 = 上車明顯比走快、能跑去採集圈探索、下車回原樣**。

## 給各線的具體指示

- **feature**:**武器已完成、別再碰**(整套已在 main)。**停堆 1-E/1-F 戰鬥/騎乘的不變式
  遍歷測試**(已飽和)、**別開 ws/game/protocol 接線 PR**(接線走維護者)。真要動就做**小而能
  獨立 land**的事:幫擴地/載具補小純函式測試、或非接線內部清理。別再造「不接線」大系統純邏輯。
- **frontend**:HUD 星露谷 dock 化已 land,非接線可玩面已飽和。**武器接線一上線就做兩件小事:
  ①「手上武器」HUD 小圖示(有武器才亮);②「怎麼合武器/武器更強」一行提示**(做完就停)。
  別再補小地圖圖例/多快捷鍵/微調既有面板——邊際趨零。擴地後補「買地確認/新地塊視覺」、載具後
  補「上下車提示」。新快照欄位一律 `|| []` 防呆。
- **backend**:武器零件你已 land 完(漂亮)。武器接線是 `game.rs` 迴圈、屬維護者一行的事,
  **別搶、別開接線 PR**。基本待命;真要動挑能獨立 land 的小事(純函式測試補強、非接線清理)。
- **feedback**:戰鬥/合成/操作說明提示已足夠,**停手**。武器接線上線後補「怎麼合武器/武器
  讓打怪更強」、擴地補「乙太怎麼買地」、載具補「怎麼上車」。在那之前別微打磨既有提示。

## 暫緩

- 再寫任何武器邏輯——整套(變體/配方/查表/測試)已在 main,只差 game.rs 一行接線。
- 再堆不接線的大系統純邏輯——零件已遠超接線速度,純囤。
- 再加一輪「健全性/無死路/不變式」遍歷測試(敵人表/配方/節點/掉落/騎乘)——已飽和,純空轉。
- 再開 `ws.rs`/`game.rs`/`protocol` 的 feature 接線 PR——接線只走維護者、一次一條。
- 再加新「產出來源」(更多採集/掉落/農作)——先把武器/擴地接通讓素材與乙太有去處再說。
- 重做已 land 的 HUD/合成台/背包/血條/採集 juice/小地圖——已足夠,讓空間給接線。
- 把武器/擴地/載具塞進同一 PR——薄切片一條條來,範圍蔓延仍是頭號殺手。
- 救卡衝突舊 PR——別 rebase,需要的檔從 main 開乾淨枝重接。
- 飛船/多星球/競速——真正遠期(載具 1-E 屬 Phase 1、可做,別跟遠期混)。
