# ButFun — 現在主攻(Planner 導航)

> 四條工程線**開工前先讀 30 秒**的當前優先序。
> **總監定調:衝可玩、一直做下去。** 唯一守「薄切片」。

## ✅ 校準(2026-06):武器接線**早已生效**——別再當待辦、更別誤接第二條傷害路徑

> 本檔原本把「武器接線」列為第一主攻、聲稱 `game.rs:174` 寫死 `PLAYER_ATTACK_POWER`、
> `combat::weapon_power` 零呼叫端待接。**核對現況後確認此段已過時**:
>
> - `PLAYER_ATTACK_POWER` 常數**在程式碼中已不存在**(只剩 `combat.rs` 幾行過時註解)。
> - 玩家攻擊改成由 `ws.rs` 的主動攻擊 handler 結算,傷害走
>   `equipment::equipped_weapon_power`(**顯式裝備槽模型**:武器基礎攻擊力 + 精煉 + 附魔)。
>   武器/護甲/精煉/附魔全部**已生效並持久化**(`equipment.rs`/`refinement.rs`)。
> - 舊的 `combat::weapon_power(&inventory)`(自動取背包最強武器的模型)**已被取代、為死碼**,
>   僅剩單元測試引用(鎖「有武器→更痛」這條設計不變式)。
> - **⚠️ 千萬別再「接武器那一行」**:若把 `combat::weapon_power` 硬接回遊戲迴圈,會與現行
>   `equipped_weapon_power` 並存成**第二條傷害路徑**,造成雙重計算/衝突(實打實的回歸 bug)。

## 🎯 現在主攻:待重新定調(下列候選需先對現況確認)

> ⚠️ 本檔以下「擴地(0-G-O2)」「載具(1-E)」等優先序**早於當前 main、尚未逐條核對**,
> 可能同樣過時。**開工前請先 grep 實際 `ClientMsg`/handler 確認該切片是否已做**,再決定主攻,
> 別照舊文字直接動工。下列保留為「候選方向」而非「確定待辦」:

### 候選方向(動工前先確認是否已做)
1. ~~武器接線~~ **已完成**(走 equipment slot,見上方校準)。
2. **擴地(0-G-O2)〔候選·待確認〕**:`protocol` 加 `ClientMsg::BuyExpansion`、`ws.rs` 收下、
   `game.rs` 查 `economy::expansion_cost` 扣乙太、給該玩家多一塊相鄰地塊。
   （注意:現有 `warehouse::buy_expansion` 是**倉庫格位**擴充,與此「買地塊」是兩回事,別混。）
3. **載具(1-E)上下車〔候選·待確認〕**:`vehicle.rs` 已在 main(728 行);上下車意圖接線
   是否已接需核對 `ws.rs`。給世界移動爽感 + 探索。

## 接下來 1-3 個切片(每個附「驗收=玩家能做到什麼」)

1. ~~武器接線~~ **已完成**:身上裝備武器(裝備槽)後攻擊明顯更痛,精煉/附魔再疊。
   驗收已達成——戰鬥早已有玩家選擇,**這條不再是待辦**(見頂部校準)。
2. **擴地 MVP(0-G-O2)— 乙太的去處〔候選·待確認〕**:固定下一格、逐格漲價、無市場。
   **驗收 = 玩家花乙太多拿一塊相鄰地塊**,收成/打怪的乙太終於有去處(GDD「產出要有去處」閉合)。
3. **載具 MVP(1-E)— 移動爽感**:走近按鍵上車、3 倍速+輕微甩尾、下車回走路。先一台車一人騎。
   **驗收 = 上車明顯比走快、能跑去採集圈探索、下車回原樣**。

## 給各線的具體指示

- **feature**:**武器已完成、別再碰**(整套已在 main)。**停堆 1-E/1-F 戰鬥/騎乘的不變式
  遍歷測試**(已飽和)、**別開 ws/game/protocol 接線 PR**(接線走維護者)。真要動就做**小而能
  獨立 land**的事:幫擴地/載具補小純函式測試、或非接線內部清理。別再造「不接線」大系統純邏輯。
- **frontend**:武器 HUD pill + 合成台/背包顯示 + 「合武器更痛」引導**已全部 land、別再碰**。
  非接線可玩面**已徹底飽和**——別再補小地圖圖例/多快捷鍵/微調既有面板/再加武器提示,邊際趨零。
  武器(裝備槽)🗡️ pill 與傷害表現**已一致、已上線**。擴地後補「買地確認/新地塊視覺」、
  載具後補「上下車提示」。新快照欄位一律 `|| []` 防呆。
- **backend**:武器(裝備槽/精煉/附魔)**整套已上線**,別再「接武器那一行」(會誤造第二條傷害路徑)。
  真要動挑能獨立 land 的小事(純函式測試補強、非接線清理)。
- **feedback**:戰鬥/合成/操作說明提示已足夠,**停手**。武器(裝備/精煉/附魔)已上線,提示可補
  「怎麼裝備/精煉武器讓打怪更強」;擴地/載具確認已做後再補對應提示。在那之前別微打磨既有提示。

## 暫緩

- 再寫任何武器邏輯——整套(變體/配方/查表/裝備槽接線/精煉/附魔)**已全在 main 且生效**,
  別再「接武器那一行」(會誤造第二條傷害路徑)。
- 再堆不接線的大系統純邏輯——零件已遠超接線速度,純囤。
- 再加一輪「健全性/無死路/不變式」遍歷測試(敵人表/配方/節點/掉落/騎乘)——已飽和,純空轉。
- 再開 `ws.rs`/`game.rs`/`protocol` 的 feature 接線 PR——接線只走維護者、一次一條。
- 再加新「產出來源」(更多採集/掉落/農作)——先把武器/擴地接通讓素材與乙太有去處再說。
- 重做已 land 的 HUD/合成台/背包/血條/採集 juice/小地圖——已足夠,讓空間給接線。
- 把武器/擴地/載具塞進同一 PR——薄切片一條條來,範圍蔓延仍是頭號殺手。
- 救卡衝突舊 PR——別 rebase,需要的檔從 main 開乾淨枝重接。
- 飛船/多星球/競速——真正遠期(載具 1-E 屬 Phase 1、可做,別跟遠期混)。
