# ButFun — Rust backend build artifacts
/target

# wasm 建置產物（scripts/build-wasm.sh 產生；deploy.sh 每次上線重建，不入 repo）
/web/wasm/

# Environment / secrets
.env

# Local Postgres data dir (dev)
/.pgdata

# Runtime data (玩家建議等，伺服器執行時產生)
/data

# OS / editor
.DS_Store
.idea/
.vscode/
.claude/
*.iml
node_modules/
package-lock.json
/scripts/e2e/node_modules

# 所有 env 變體都不入 repo
.env*
!.env.example
